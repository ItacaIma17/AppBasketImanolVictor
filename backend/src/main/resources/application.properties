spring.application.name=AppBasketImanolVictor

# Base de datos
# En local (Docker): usa postgres:5432
# En Render: DB_HOST, DB_PORT, DB_NAME se inyectan desde la BD gestionada
spring.datasource.url=jdbc:postgresql://${DB_HOST:postgres}:${DB_PORT:5432}/${DB_NAME:fabaragon_db}
spring.datasource.username=${DB_USER:admin}
spring.datasource.password=${DB_PASSWORD:Admin17#}
spring.datasource.driver-class-name=org.postgresql.Driver
server.address=0.0.0.0
server.port=8080
# Jackson
spring.jackson.serialization.write-dates-as-timestamps=false

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
jwt.secret=${JWT_SECRET:tu_clave_secreta_muy_segura_para_desarrollo_2024}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Email (para verificacion)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:imanollapiozondo@gmail.com}
spring.mail.password=${MAIL_PASSWORD:oxrscakwredlmuxn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuracion del administrador (lee de variables de entorno)
admin.username=${ADMIN_USERNAME:admin}
admin.email=${ADMIN_EMAIL:admin@admin.com}
admin.password=${ADMIN_PASSWORD:Admin123456}
admin.nombre=${ADMIN_NOMBRE:Administrador}
admin.apellido=${ADMIN_APELLIDO:Sistema}
admin.secret.key=${ADMIN_SECRET_KEY:ADMIN_SECRET_KEY_2024}


# Usuarios por defecto
default.entrenador.username=${DEFAULT_ENTRENADOR_USERNAME:entrenador}
default.entrenador.email=${DEFAULT_ENTRENADOR_EMAIL:entrenador@test.com}
default.entrenador.password=${DEFAULT_ENTRENADOR_PASSWORD:Entrenador123}
default.entrenador.codigo=${DEFAULT_ENTRENADOR_CODIGO:ENT-100001}

default.arbitro.username=${DEFAULT_ARBITRO_USERNAME:arbitro}
default.arbitro.email=${DEFAULT_ARBITRO_EMAIL:arbitro@test.com}
default.arbitro.password=${DEFAULT_ARBITRO_PASSWORD:Arbitro123}
default.arbitro.codigo=${DEFAULT_ARBITRO_CODIGO:ARB-100001}

default.jugador.username=${DEFAULT_JUGADOR_USERNAME:jugador}
default.jugador.email=${DEFAULT_JUGADOR_EMAIL:jugador@test.com}
default.jugador.password=${DEFAULT_JUGADOR_PASSWORD:Jugador123}
default.jugador.codigo=${DEFAULT_JUGADOR_CODIGO:JUG-100001}

# Subida de archivos (actas PDF/imagen)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB